<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="kr.co.T2Market.mapper.ReplyMapper">
 
 
  	<!-- 댓글등록 -->
  	<insert id="enrollReply">
  	
  		insert into review (reply, score) values(#{reply}, #{score})
  	
  	</insert>
  </mapper>