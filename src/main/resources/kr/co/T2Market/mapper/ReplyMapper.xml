<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="kr.co.T2Market.mapper.ReplyMapper">
 
 	<!-- 댓글 조회 -->
   	<select id="list" parameterType="String" resultType="kr.co.T2Market.domain.ReplyVO">
  	
  		select * from review where product_no = #{product_no}
  	</select>
 
  	<!-- 댓글등록 -->
  	<insert id="write" parameterType="kr.co.T2Market.domain.ReplyVO">
  	
  		insert into review (review_no, product_no, member_id) 
  		values(rev_seq.nextval, #{product_no},'TEST')
  		
  		
  	
  	</insert>
  	
  	<!-- 댓글 수정 -->
  	<update id ="modify" parameterType="kr.co.T2Market.domain.ReplyVO">
  	
  		update review set 
  		reply = #{reply}, 
  		updatedate = sysdate
  		where product_no = #{product_no}
  	</update>  	

  	
  	<delete id ="delete" parameterType="kr.co.T2Market.domain.ReplyVO">
  	
  		delete from review where product_no = #{product_no}
  		and member_id='TEST'
  		
  	</delete>

  </mapper>